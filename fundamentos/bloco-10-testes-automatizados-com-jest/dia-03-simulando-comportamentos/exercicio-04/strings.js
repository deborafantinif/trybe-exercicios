const uppercase = (string) => string.toUpperCase();

const firstLetter = (string) => string[0];

const concatWords = (word1, word2) => word1 + word2;

module.exports = { uppercase, firstLetter, concatWords };